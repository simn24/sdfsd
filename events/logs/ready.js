const { PREFIX } = require('../../config');

module.exports = client => {

console.log(`Je suis près ! Mon préfix : ${PREFIX}`)
console.log('Jopère actuellement sur le serveur : ${serveur}')
   discordBot.user.setActivity('Vous observe :=');
 discordBot.user.setStatus('idle');

}
